server:
  port: 10000
spring:
  application:
    name: ly-registry
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10000/eureka
    fetch-registry: false
    register-with-eureka: false